# Preferencias del proyecto

- Cualquier enlace que apunte a un ancla dentro de la misma página (p. ej. `#contacto`, `#faq`, `#simulador`) debe hacer scroll suave. Se implementa con `html{ scroll-behavior:smooth; }` en el `<style>` de cada página. Añadir esta regla a cualquier página nueva que se cree.
